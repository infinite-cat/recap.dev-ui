import React, { memo } from 'react'

import { PageHeader } from '../components'

const UnitsContainer = memo(() => (
  <PageHeader title="Units" subTitle="List of all your units">
    Units
  </PageHeader>
))

export default UnitsContainer
